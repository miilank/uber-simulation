<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/white"
    android:orientation="vertical"
    android:padding="20dp">

    <!-- Pickup autocomplete container -->
    <com.example.mobileapp.features.shared.input.LocationSearchInputView
        android:id="@+id/pickup"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp" />

    <!-- Destination autocomplete container -->
    <com.example.mobileapp.features.shared.input.LocationSearchInputView
        android:id="@+id/destination"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp" />

    <!-- Search Button -->
    <Button
        android:id="@+id/get_estimate_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Get Estimate"
        android:backgroundTint="@color/app_accent"
        android:textColor="@android:color/black"
        android:textSize="20sp"
        android:fontFamily="@font/poppins_semi_bold"/>
</LinearLayout>
